[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'No build step needed - using tsx'"]

[start]
cmd = "npx tsx packages/execution-engine/src/index.ts"
